name: CI
on: push
jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install npm modules
      run: npm install
    - name: Run ESLint
      run: npm run lint
